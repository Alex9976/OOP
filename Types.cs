namespace OOP
{
    enum TEngType { petrol = 1, diesel, gas, electricity }
    enum TEngine { jet = 1, turboprop }
    enum TPurpose { multipurpose = 1, passenger, transport, search, agricultural }
    enum TTravelWay { automotive = 1, towed, alloyed, drifting }
}
