namespace OOP
{
    public enum TEngType { petrol = 1, diesel, gas, electricity }
    public enum TEngine { jet = 1, turboprop }
    public enum TPurpose { multipurpose = 1, passenger, transport, search, agricultural }
    public enum TTravelWay { automotive = 1, towed, alloyed, drifting }
}
